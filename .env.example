# Export this value through your process manager if a custom directory is needed.
# GARMINTOKENS=/path/to/dedicated/token/directory  # Default: ~/.garminconnect

# Garmin mutations are disabled by default. Enable only the exact capabilities needed.
# GARMIN_WRITE_CAPABILITIES=weight.write,health.hydration
